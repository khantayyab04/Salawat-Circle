# Shared Types

Gemeinsam genutzte TypeScript-Typen und generierte Datenbanktypen.

`src/database.types.ts` wird ausschließlich mit `pnpm supabase:types` aus dem lokalen,
vollständig migrierten Supabase-Schema erzeugt. Die Datei nicht manuell bearbeiten.
